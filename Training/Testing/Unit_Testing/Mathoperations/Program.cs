
using Calculation;
//using Mathoperations;
    //im add reference from the calculation class library
        // in test project it shows error in mathoperation class is not there!
            CalaculationClass calculation = new CalaculationClass(20,20);
            Console.WriteLine( calculation.Add());
            Console.WriteLine(calculation.Sub());

